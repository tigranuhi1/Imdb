package com.aca.imdb.engine.movie;

import java.time.LocalDate;

class Animation extends Movie  {
    Animation(String title, String description, LocalDate premierDate) {
        super(title, description, premierDate);
    }
}
